# Redireciona a raiz para o index sem mostrar .html
/ /index.html 200

# Redireciona /index para / (remove o index.html)
/index / 301

# Redireciona /index.html para / (remove o .html)
/index.html / 301

# Páginas não encontradas vão para a home (404 amigável)
/* /index.html 404
